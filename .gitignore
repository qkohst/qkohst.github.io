# Playwright MCP session artifacts
.playwright-mcp/

# OS cruft
Thumbs.db
desktop.ini
.DS_Store

# Alat build lokal — tidak pernah ter-deploy
tools/node_modules/
_preview/
data/projects.draft.json

# Sumber proyek: gambar asli disimpan, hasil optimasinya di assets/img/
# (folder content/ ikut di-commit — ia sumber kebenaran untuk proyek baru)

# Tangkapan layar ASLI yang masih memuat data pribadi. Hanya hasil samaran di
# content/redacted/ yang boleh ter-commit; berkas di sini dipakai sebagai bahan
# oleh tools/redact-images.js dan tidak pernah terbit.
content/_raw/

# Pengaturan alat bantu lokal, bukan bagian dari situs.
.claude/
